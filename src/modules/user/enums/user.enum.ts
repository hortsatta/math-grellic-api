export enum UserApprovalStatus {
  MailPending = 'mail-pending',
  Pending = 'pending',
  Approved = 'approved',
  Rejected = 'rejected',
}

export enum UserRole {
  Admin = 'admin',
  Teacher = 'teacher',
  Student = 'student',
}

export enum UserGender {
  Male = 'male',
  Female = 'female',
}
